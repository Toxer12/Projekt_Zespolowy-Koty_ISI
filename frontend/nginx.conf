server {
    listen 80;

    location /admin/ {
        return 444;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}