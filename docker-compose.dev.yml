services:
  app:
    env_file:
      - .env.dev
    ports:
      - "8000:8000"
    volumes:
      - ./backend/src:/src
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]